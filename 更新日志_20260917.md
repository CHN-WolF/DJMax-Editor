# 更新日志 2026-09-17

记录范围：自上一份全量备份（DJMaxEditor_full_backup_20260916_172839.zip，2026-09-16 17:28）之后的改动。

## 单实例打开谱面 + 文件关联（提交 9dc205e，2026-09-16 20:40）

- 二次启动不再开新实例：通过命名管道把文件参数转发给已运行的进程，由其在标签页中打开并将窗口置前。
- 同时冷启动（资源管理器多选打开）会重试获取单实例所有权，避免并行编辑器；已失效的占用者可干净交接。
- 启动支持多个文件参数（此前仅识别第一个）。
- 每次运行刷新 HKCU 下所有可打开谱面格式（.pt / .bytes / .xml / .bms / .bme / .bml / .pms / .tech）的按用户文件关联，指向当前 exe；双击谱面即可进入单实例。注册无需管理员权限，且不阻塞启动。
- 新增文件：`DJMaxEditor/SingleInstance.cs`、`DJMaxEditor/FileAssociations.cs`
- 修改文件：`DJMaxEditor/Program.cs`、`DJMaxEditor/MainForm.cs`、`DJMaxEditor/DJMaxEditor.csproj`
- 改动量：5 个文件，+515 / -11

## Debug Output 面板不显示日志修复（提交 196067e，2026-09-17）

- 根因：`Logs.Write` 方法体被 `#if DEBUG` 包裹（`DJMaxEditor/Logs.cs`），Release 编译下为空方法，`OnLogWrite` 不触发，Debug Output 面板无任何输出。
- 修复：移除 `#if DEBUG` / `#endif`，Debug 与 Release 均输出到面板；日志调用点很少，性能影响可忽略。
- Release 完整构建已通过并部署到 `DJMaxEditor/bin/Release/DJMaxEditor.exe`。
- 仓库已推送：`master 9dc205e..196067e` → `origin`（github.com/CHN-WolF/DJMax-Editor），并建立 upstream 跟踪。

## Ctrl+F 按音效找音符 + UI 图标刷新（提交 88782b3，2026-09-17 12:23）

- 新增「按音效查找音符」对话框（Ctrl+F）：按音效文件名搜索引用该音效的音符，可编辑下拉框 + 结果表格，Find Next/Previous 会滚动编辑器定位并选中该音符。
- Core 新增 `SoundNoteSearch` 领域辅助类及单元测试；`IEditorSurface.RevealPosition` 在 Legacy 与 TimelineV2 两套编辑器表面均已实现。
- File 菜单接入 Show Save 项（SaveCurrentDocument）。
- 菜单与工具栏图标替换为提取的 WPS Office 图标，部分资源为深色主题高清重绘（fmod、properties、DebugOutput、编辑器事件精灵、cinema projector videoStart）。
- 改动量：48 个文件，+926 / -52。

## csproj 编译项修复（2026-09-17，推送前）

- `DJMaxEditor/DJMaxEditor.csproj` 中 `FindSoundNotesForm.cs` 的 `<Compile>` 项一度丢失（MainForm 引用该类，缺失会导致编译失败），已恢复为 `<SubType>Form</SubType>`。
- 同时保留 VS 自动调整的设计器元数据：`GameplayPreviewControl` 的 SubType 改为 Component、`TechPatternPickerForm` 标记为 Form。
- Release 完整构建已通过（仅有既有警告），产物 `DJMaxEditor/bin/Release/DJMaxEditor.exe`。

## 备份

- 本次全量备份文件：同目录下 `DJMaxEditor_full_backup_20260917_*.zip`（含本日志；不含 `.git`、`.vs` 及历史备份 zip，与既有备份惯例一致）。
